<%= form_for(@business_type) do |f| %>
  <% if @business_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@business_type.errors.count, "error") %> prohibited this business_type from being saved:</h2>

      <ul>
      <% @business_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :business %>
    <%= f.text_field :business %>
  </div>
  
  <% for expense in ExpenseType.find(:all) %>
  
  <%= check_box_tag "business_type[expense_type_ids][]", expense.id, @business_type.expense_types.include?(expense) %>
  <%= expense.expense %><br />
  
  <% end %>
  
  
  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
